defmodule JackFrostBotTest do
  use ExUnit.Case

  test "the truth" do

#    JackFrostBot.FileCommand.read("data/out.txt")
#    |> Enum.map(fn(str) ->
#
#      String.split(str, " ")
#      |> JackFrostBot.Ngram.tri_ngram()
#    end)
#    |> IO.inspect()

    assert 1 + 1 == 2
  end

end
